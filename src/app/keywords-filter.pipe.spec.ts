import { KeywordsFilterPipe } from './keywords-filter.pipe';

describe('KeywordsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KeywordsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
